const styles = (() => ({
    appwrapper_container: {
        height: '100vh',
        weidth: '100vw',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
    },
}));

export default styles;
